[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "voiceguard"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai-agents",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
]
